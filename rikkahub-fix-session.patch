diff --git a/ai/src/main/java/me/rerere/ai/util/Request.kt b/ai/src/main/java/me/rerere/ai/util/Request.kt
index 9d6013a..dd17f23 100644
--- a/ai/src/main/java/me/rerere/ai/util/Request.kt
+++ b/ai/src/main/java/me/rerere/ai/util/Request.kt
@@ -42,7 +42,12 @@ fun Request.Builder.configureSessionHeaders(url: String, sessionId: String?): Re
     if (sessionId != null) {
         header("X-Session-ID", sessionId)
         if (url.toHttpUrl().host == "opencode.ai") {
-            header("x-opencode-session", sessionId)
+            // Zen free tier requires x-opencode-session: ses_ + 26 alnum chars.
+            // Don't clobber a user-supplied custom header; format ours to match.
+            val custom = runCatching { build().header("x-opencode-session") }.getOrNull()
+            if (custom == null) {
+                header("x-opencode-session", "ses_" + sessionId.replace("-", "").take(26))
+            }
         }
     }
 }
